# gRPC + Arrow Flight transports. >=1.3.0 is required for the Arrow Flight
# transport; the [arrow] extra pulls in pyarrow.
databricks-zerobus-ingest-sdk[arrow]>=1.3.0
databricks-sdk>=0.30.0
rich>=13.0.0
PyYAML>=6.0
# Used by the REST transport (also pulled in transitively by databricks-sdk).
requests>=2.28.0
